import 'package:flutter/material.dart';

class EntitiesView extends StatelessWidget {
  const EntitiesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Entities")),
    );
  }
}
