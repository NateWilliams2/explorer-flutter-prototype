import 'package:flutter/material.dart';

class ProcessesView extends StatelessWidget {
  const ProcessesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Processes")),
    );
  }
}
