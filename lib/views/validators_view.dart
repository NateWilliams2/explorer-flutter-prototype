import 'package:flutter/material.dart';

class ValidatorsView extends StatelessWidget {
  const ValidatorsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Validators")),
    );
  }
}
