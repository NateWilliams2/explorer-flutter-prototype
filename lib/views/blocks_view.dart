import 'package:flutter/material.dart';

class BlocksView extends StatelessWidget {
  const BlocksView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Blocks")),
    );
  }
}
